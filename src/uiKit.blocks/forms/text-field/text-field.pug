mixin text-field(options)
    .text-field
        if options.name == 'Text field'
            h3= options.name        
            p Default
        else 
            h3= options.name
        input(type= options.type name= options.name placeholder= options.placeholder)